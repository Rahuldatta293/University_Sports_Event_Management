{
	"name": "app",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "",
	"main": "index.js",
	"scripts": {
		"setup": "run-p setup:*",
		"setup:backend": "(cd backend && dotnet build)",
		"setup:frontend": "npm install --prefix frontend",
		"start": "run-p start:*",
		"start:backend": "(cd backend && dotnet run)",
		"start:frontend": "npm run dev --prefix frontend"
	},
	"dependencies": {
		"npm-run-all": "^4.1.5"
	}
}
